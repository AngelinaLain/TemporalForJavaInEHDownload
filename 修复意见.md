# 🎨 GalleryImport 系统后续完善与优化方案

> 2026-07-08 审计更新：已落地项（Task Queue 三队列隔离、爬虫 Activity 心跳、精细化 RetryOptions/doNotRetry、爬虫异常分类+致命错误不可重试+报警、库扫描缓冲期/KomgaImportWorkflow）已移除。
> 以下仅列出**未完成**及**部分完成需增强**的项目。

## 1. 架构与通信

### 未完成
- **事件总线解耦**：通知（`NotificationActivity`）可从 Temporal 编排中解耦，引入 Spring ApplicationEvent 或 Redis Pub/Sub 异步发布，降低耦合。
- **Synology Activity 心跳重构**：当前群晖长轮询在 Workflow 层用 `Workflow.sleep` + `checkSynologyTaskStatus` 实现（可工作，但轮询逻辑散落在工作流里）。建议按原设计将轮询下沉到 `SynologyActivityImpl` 内部：`StartToCloseTimeout=48h` + `HeartbeatTimeout=5min` + 进度上报，在 Temporal UI 中可实时看到下载进度。

## 2. 爬虫与网络请求层

### 已部分完成
- **索引**：`gid`(PK) + `idx_download_status` 已建。需补 `token` 索引（`findBookInKomga` 等场景按 token 查询）。
- **批量 DB 操作**：`saveGalleriesBatch` / `getGalleriesByIds` 已有。需优化为 MyBatis-Plus `insertBatchSomeColumn` 提升大批量写入性能。

### 未完成
- **智能限流**：在 `EhNetworkClient` 层引入 Resilience4j RateLimiter（令牌桶），对 e-hentai API 做全局请求速率控制。当前仅靠 `gid % 10` 抖动 + Temporal 退避兜底。
- **动态代理池**：`EhNetworkConfig` 目前单一静态代理。需支持多代理轮换池，遇到封禁（403/502）自动切换。
- **Cookie/账号轮转池**：多账号 Cookie 池轮询分散单账号频率。当前仅一套 Cookie。
- **下载断点续传**：在 `ArchiveDownloadInfo` 相关逻辑实现 HTTP Range 分块下载和断点续传。
- **虚拟线程**：升级到 Java 21+ 后，网络 I/O 密集型任务启用虚拟线程降上下文切换开销。

## 3. 业务逻辑与第三方集成

### 已部分完成
- **AI 服务降级**：已有 `ServiceUnavailable → 503 → Temporal 重试`。需补充本地离线词典兜底：当 AI 端点连续失败时，自动降级为本地 EH Tag 翻译库匹配或直接透传英文 Tag。

### 未完成
- **Komga 批量 API**：`KomgaApiClient` 当前单本逐条 PATCH 元数据。应合并多本书的元数据更新请求，减少 HTTP 交互次数。
- **Komga 元数据幂等操作**：`pushMetadataToKomga` 直接 PATCH，未做 GET 比对。应在写入前先 `GET /api/v1/books/{bookId}` 检查现有元数据，如果已有预期 Tag 则跳过更新。如此 Temporal 重试不会产生重复标签。
- **Komga 增量同步**：`KomgaSyncService` 应从全量扫描改为基于时间戳/Hash 的增量比对。
- **AI Prompt 批量翻译**：将多个 Tag 合并为一个 Prompt 批量请求，节约 Token 并降低延迟。
- **AI Redis 缓存前置**：`EhTagTranslationService` 当前用 Caffeine 内存缓存（24h）。迁移到 Redis 实现跨实例共享，翻译过的 Tag 命中率 >80%。
- **AI Resilience4j 断路器**：引入熔断器，AI 端点连续超时/报错触发熔断 → 降级到本地词典。

## 4. 容错性与数据库

### 已部分完成
- **批量 DB**：见第2节批量 DB 操作。
- **索引**：见第2节索引。

### 未完成
- **PARTIAL 状态 + 页数校验**：`DownloadStatus` 枚举需新增 `PARTIAL` 状态。抓取完成后校验 `预计总页数` vs `实际抓取数`，不匹配标记为 Partial 待人工审核。

## 5. 安全性

### 未完成
- **JWT 黑名单**：基于 Redis 实现 Token 黑名单，支持注销/改密后即时失效。
- **细粒度权限**：`DashboardController` / `EHAutomationController` 敏感端点增加 `@PreAuthorize` 角色校验。当前全项目 0 处使用。
- **配置加密**：`application-*.yaml` 中 NAS/Proxy/AI/Graph/Komga 等凭证以明文存储（虽用 `${ENV:default}` 占位，但默认值就是明文）。引入 Jasypt 进行配置加密，或接入外部 Secrets Manager。

## 6. 可观测性

### 异常项
- **Actuator 依赖缺失**：`SecurityConfig` 已放行 `/actuator/**`，但 POM 中未引入 `spring-boot-starter-actuator`，端点不生效。

### 未完成
- **Micrometer Tracing + Zipkin**：为 `main-service` / `scraper-worker` / `ai-service` 注入 `traceId` 串联跨服务调用链。
- **日志集中**：接入 ELK (Elasticsearch + Logstash + Kibana) 或 Loki 汇聚业务日志。
- **Prometheus + Grafana**：暴露 Prometheus metrics（JVM、Temporal 任务积压、下载失败率、AI 耗时/成本）。

## 7. 部署

### 已部分完成
- **docker-compose**：已编排 `main-service`(backend) + Vue 前端。需补充 `scraper-worker` 和 `ai-service` 容器，达成一键全栈部署。

## 🚀 Roadmap（更新）

### Phase 1：稳定性补全
1. `EhNetworkClient` 加入 Resilience4j RateLimiter 防封。
2. Komga 元数据幂等操作（PATCH 前 GET 比对）。
3. `DownloadStatus` 增加 PARTIAL + 页数校验。
4. token 字段添加数据库索引。

### Phase 2：性能与缓存
1. 迁移至 Redis 缓存层（AI Tag 翻译第一优先级）。
2. 动态代理池 / Cookie 轮转。
3. AI Prompt 批量翻译 + Resilience4j 断路器 + 本地词典降级。
4. Komga 批量 API + 增量同步。

### Phase 3：安全与可观测
1. JWT 黑名单 + `@PreAuthorize` 细粒度权限。
2. Jasypt 配置加密。
3. Actuator + Prometheus + Grafana 监控大盘。
4. Micrometer Tracing + ELK/Loki 日志汇聚。
5. docker-compose 补充全栈容器编排。
