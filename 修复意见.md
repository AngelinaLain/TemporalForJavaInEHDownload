# 🎨 GalleryImport 系统后续完善与优化方案

> 2026-08-17 审计更新：本次已落地以下项目（详见各节「✅ 已落地」标注），剩余项见各节末尾。

## 1. 架构与通信

### ✅ 已落地
- **事件总线解耦**：`NotificationActivity` 不再直接调用 Graph API，改为发布 `NotificationEvent`（Spring ApplicationEvent），由 `NotificationEventListener` 在后台线程池异步消费（`EmailNotificationService` 负责实际发送）；配置 Redis 时额外向 Pub/Sub 频道 `app:notifications` 广播，供跨实例扩展。
- **Synology Activity 心跳重构**：轮询逻辑已下沉到 `SynologyActivityImpl.waitForDownloadComplete`，`StartToCloseTimeout=48h` + `HeartbeatTimeout=5min`（见 `WorkflowSteps.SYNO_LONG_OPTIONS`），Activity 内通过 `Activity.getExecutionContext().heartbeat(...)` 上报进度，Temporal UI 可实时查看下载进度；工作流不再 `Workflow.sleep` 轮询。

## 2. 爬虫与网络请求层

### ✅ 已落地
- **索引**：`gid`(PK) + `idx_eh_galleries_download_status` + `idx_eh_galleries_token`（V2）+ `idx_eh_galleries_status_crawled_at` 均已建，token 查询场景已有索引覆盖。
- **批量 DB 操作**：`saveGalleriesBatch` 已改造为「新记录走 `insertBatchSomeColumn`（`BatchSqlInjector` 注入，一次 INSERT 多行）+ 已有记录走 `saveOrUpdateBatch`」，大批量写入性能显著提升。
- **智能限流**：`EhNetworkClient` 已引入 Resilience4j RateLimiter（令牌桶，配置 `eh-config.rate-limit`），对 e-hentai API 做全局请求速率控制。
- **动态代理池**：`EhNetworkConfig` 新增 `proxy-pool`，`EhNetworkClient` 支持多代理轮换，遭遇 403/502 自动冷却当前代理并切换（`proxy-cooldown-seconds`）。
- **Cookie/账号轮转池**：`EhNetworkConfig` 新增 `cookie-pool`，多账号 Cookie 轮询分散单账号频率；Cookie 失效自动切换下一账号重试。
- **下载断点续传**：`EhNetworkClient.downloadWithResume` 实现 HTTP Range 分块下载 + `.part` 断点续传，已接入 `EhTagTranslationServiceImpl` 的 db.text.json 启动下载。
- **虚拟线程**：新增 `VirtualThreadConfig`（Java 21+ 反射启用虚拟线程执行器 + Tomcat 协议处理器，Java 17 自动回退平台线程池），`spring.threads.virtual.enabled` 默认关闭、Java 21 可开。

## 3. 业务逻辑与第三方集成

### ✅ 已落地
- **AI 服务降级**：`AiSummaryService` 引入 Resilience4j 断路器，AI 端点连续失败熔断 → 自动降级为本地 EH Tag 翻译库匹配 / 英文 Tag 透传；瞬时 503 仍上抛交 Temporal 重试。
- **Komga 批量 API**：`KomgaApiClient.patchBookMetadataBatch` 以受限并发异步 PATCH（复用 HTTP keep-alive），替代逐本串行 + `Thread.sleep`。
- **Komga 元数据幂等操作**：`KomgaApiClient.patchBookMetadataIfChanged` 在 PATCH 前先 `GET /api/v1/books/{bookId}` 比对标题/标签/作者/摘要，一致则跳过，Temporal 重试不再产生重复标签。
- **Komga 增量同步**：`KomgaSyncServiceImpl` 基于 `app_sync_checkpoint` 时间戳 + `updated_at` 列做增量比对，写入前再按 Komga 现有标签做 Hash 比对跳过。
- **AI Prompt 批量翻译**：ai-service 新增 `POST /api/ai/batch-translate-tags`，多 Tag 合并单 Prompt；`EhTagTranslationService.translateBatch` + `AiTagTranslationService` 串联批量翻译。
- **AI Redis 缓存前置**：`EhTagTranslationServiceImpl` 翻译结果写入 Redis 共享缓存（TTL 24h，跨实例命中），Redis 不可用时自动降级内存。
- **AI Resilience4j 断路器**：`aiSummary` / `aiTagTranslation` 两个断路器实例（`resilience4j.circuitbreaker` 配置）熔断后降级本地词典。

## 4. 容错性与数据库

### ✅ 已落地
- **PARTIAL 状态 + 页数校验**：`DownloadStatus` 新增 `PARTIAL`（不完整）；`SingleGalleryDownloadWorkflowImpl.isPartialDownload` 在下载完成后校验「预估大小 vs 实际大小（不足 60%）」与「平均每页大小（<100KB）」判定不完整，标记 PARTIAL 并邮件告警待人工审核；`getFailedGalleries` 纳入 PARTIAL 便于重试。前端状态与仪表盘 SQL 同步支持 PARTIAL。

## 5. 安全性

### ✅ 已落地
- **JWT 黑名单**：`JwtTokenProvider` 生成带 `jti`/`roles` 声明的 Token；新增 `POST /api/auth/logout`，`TokenBlacklistService`（Redis + 本地缓存降级）实现注销/改密后即时失效；`JwtAuthenticationFilter` 校验黑名单。
- **细粒度权限**：`@EnableMethodSecurity` 开启，`DashboardController` / `EHAutomationController` 增加 `@PreAuthorize("hasRole('ADMIN')")`；`SecurityConfig` 增加 403 `accessDeniedHandler`。
- **配置加密**：引入 `jasypt-spring-boot-starter`，支持 `${ENC(密文)}` 占位符（`jasypt.encryptor.password=${JASYPT_PASSWORD}`），`.env.example` 已补说明。

## 6. 可观测性

### ✅ 已落地
- **Actuator 依赖缺失**：`main-service` / `scraper-worker` / `ai-service` 均已引入 `spring-boot-starter-actuator`，暴露 `health/info/prometheus/metrics`。
- **Micrometer Tracing + Zipkin**：三服务引入 `micrometer-tracing-bridge-brave` + `zipkin-reporter-brave`，`management.zipkin.tracing.endpoint=${ZIPKIN_URL}`。
- **日志集中**：`docker-compose.yml` 新增 Loki + Promtail（`deploy/promtail/promtail-config.yml` 采集 Docker 容器日志）。
- **Prometheus + Grafana**：三服务暴露 Prometheus metrics（`micrometer-registry-prometheus`），`docker-compose.yml` + `deploy/prometheus/prometheus.yml` 提供 Prometheus/Grafana。

## 7. 部署

### ✅ 已落地
- **docker-compose 全栈编排**：已含 `main-service` / `scraper-worker` / `ai-service` / Vue 前端 + 新增 Redis（默认）与 Prometheus/Grafana/Zipkin/Loki/Promtail（`observability` profile）。修复了 Dockerfile 阶段二缺失 `COPY/EXPOSE/ENTRYPOINT` 的问题。

## 🚀 剩余可优化项（本次未覆盖）

1. **虚拟线程全量验证**：需在 Java 21 运行时环境实测（本地为 JDK 17，仅提供兼容回退）。
2. **Komga 元数据 Hash 增量**的 `updated_at` 需依赖新 V4 迁移与 `MetaObjectHandler`，旧数据首次同步仍会全量。
3. **日志集中 / 监控大盘**的 Loki/Prometheus/Grafana 未在真实环境联调。
4. 断点续传当前接入 EhTag 词典下载；若需直连 e-hentai 归档下载还需改造 Synology 下载链路。
